# Memory thresholds (we'll use later)
DEFAULT_TOKEN_LIMIT = 2500
DEFAULT_TURN_LIMIT = 8

# Cost simulation
DEFAULT_COST_PER_TOKEN = 0.000002

# Load thresholds (future use)
LOW_LOAD_THRESHOLD = 5
HIGH_LOAD_THRESHOLD = 15
